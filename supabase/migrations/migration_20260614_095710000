-- Drop the FK constraint on parent_section_id to avoid race conditions during creation.
-- The parent-child relationship is maintained at the application layer.
ALTER TABLE sections DROP CONSTRAINT IF EXISTS sections_parent_section_id_fkey;
