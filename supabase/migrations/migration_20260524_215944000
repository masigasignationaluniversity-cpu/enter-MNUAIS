-- Reset admin password using proper bcrypt cost factor 10 (Supabase default)
UPDATE auth.users 
SET 
  encrypted_password = crypt('Admin@2025', gen_salt('bf', 10)),
  updated_at = NOW()
WHERE email = 'admin@ais.local';
